# Predicates

`col()` builds the conditions a `ColRule` or a `Check` carries. Unlike a raw
`pl.Expr`, a predicate built this way survives a round trip through a spec
file -- see [Specs as files](../../how-to/files.md).

## col

::: polspec.col

## Pred

::: polspec.Pred
