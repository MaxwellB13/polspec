[package]
name = "polspec"
# Placeholder. The released version is `project.version` in pyproject.toml,
# which maturin prefers over this field; nothing consumes the crate version.
version = "0.0.0"
edition = "2024"

[lib]
name = "_polspec"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1

[dependencies]
polars = { version = "0.55.2", features = [
    "dtype-categorical",
    "dtype-i8",
    "dtype-i16",
    "dtype-u8",
    "dtype-u16",
] }
polars-arrow = "0.55.2"
polars-core = { version = "0.55.2", features = [
    "dtype-i8",
    "dtype-i16",
    "dtype-u8",
    "dtype-u16",
] }
# `extension-module` is deliberately not listed here: maturin enables it via
# `[tool.maturin] features` in pyproject.toml. Enabling it unconditionally
# stops `cargo test` from linking against a Python interpreter.
# abi3-py312 matches `requires-python = ">=3.12"`.
pyo3 = { version = "0.29.2", features = ["abi3-py312"] }
pyo3-polars = { version = "0.28.0", features = [
    "dtype-categorical",
    "dtype-full",
] }
rand = "0.10"
rand_distr = "0.6"
rand_xoshiro = "0.8"
rayon = "1.12.0"
